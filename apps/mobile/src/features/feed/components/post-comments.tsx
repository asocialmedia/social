// The eddie thread moved to features/eddies (web-parity rows, tree, delete,
// attachments, floating composer). Kept as the stable import path for the
// feed card and the post screen.
export { EddieThread as PostComments } from "@/features/eddies/components/eddie-thread";
export type { EddieThreadProps as PostCommentsProps } from "@/features/eddies/components/eddie-thread";
