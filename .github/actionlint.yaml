# actionlint config — using GitHub-hosted runners only (ubuntu-latest)
# No self-hosted runner labels required; previous Blacksmith label backed up to actionlint.yaml.bak
self-hosted-runner:
  labels: []
