{
  "name": "@asm/media",
  "version": "1.0.7",
  "private": true,
  "description": "Shared media pipeline contracts: lifecycle state machine, storage layout, limits, content detection, derivative policy",
  "license": "AGPL-3.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "bun test src"
  },
  "devDependencies": {
    "@asm/config": "workspace:*",
    "@types/bun": "^1.4.0",
    "bun-types": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
